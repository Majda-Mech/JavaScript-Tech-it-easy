// Opdracht 1 - Array Methoden
// Opdracht 1a: Gebruik een array-methode om een array te maken met alle tv-type namen. Log de uitkomst in de console.
const tvTypes = inventory.map((tv) =>tv.type);
return inventory;
console.log(tvTypes)

// Opdracht 1b: Gebruik een array-methode om alle tv's te verzamelen (de hele objecten) die volledig uitverkocht zijn. Log de uitkomst in de console.
const soldOut = inventory.filter((tv) =>soldOut);
return inventory;
console.log(soldOut)

// Opdracht 1c: Gebruik een array-methode om alle tv's te verzamelen (de hele objecten) die over AmbiLight beschikken. Log de uitkomst in de console.
const tvWithAmbilight = inventory.filter((tv) =>tvWithAmbilight);
return inventorygit;
console.log(tvWithAmbilight)

// Opdracht 1d: Schrijf een functie die alle tv's van laagste naar hoogste prijs sorteert. Log de uitkomst in de console
const soldOut = inventory.sort((tv =>soldOut);
return inventory;
console.log(soldOut)